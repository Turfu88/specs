import { Box } from '@mui/material';
import Button from '@mui/material/Button';

export function Spec() {

    return (
        <Box className="d-flex justify-content-center mt-4">
            <div className="text">Spec Page</div>
        </Box>
    );
}
